@model EDDProyecto.Models.Video
@{
    ViewBag.Title = "Catalogo";
}

<h1>Catálogo</h1>

<table cellpadding="30" cellspacing="20" border="1">
    <tr>
        <th>Tipo</th>
        <th>Nombre</th>
        <th>Año de lanzamiento</th>
        <th>Género</th>
    </tr>

    @*@for (int i = 0; i < Model.Count; i++)
        {
            <tr>
                <td>@Model.ElementAt(i).Value.NombrePais </td>
                <td>@Model.ElementAt(i).Value.Grupo </td>
                <td>@Html.ActionLink("Delete", "Delete", "Paises", new { id = i }, null)</td>
            </tr>
        }*@
</table>
<p>
    @Html.ActionLink("Cargar videos", "Cargar", FormMethod.Get)
    |@Html.ActionLink("Ingresar videos", "Ingresar",FormMethod.Get)
</p>
